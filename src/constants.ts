import { join } from 'path';

export const MAIL_TEMPLATE_DIR = join(__dirname, 'templates', 'mail-templates');
export const DEFAULT_PORT = 3000;
export const DEFAULT_USER = 'admin';
export const DEFAULT_PASSWORD = 'admin';
